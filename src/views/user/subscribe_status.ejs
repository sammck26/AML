<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/style.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription Status</title>
</head>
<body>
    <!-- Include layout and sidebar -->
    <%- include('../layout.ejs', { user }) %>
    <div class="profile-page">
        <%- include('../partials/userSideBar.ejs', { user }) %>

        <main class="content">
            <!-- Display success or failure message dynamically -->
            <% if (status === 'success') { %>
                <h1>Subscription Successful!</h1>
                <p><%= message %></p>
                <p>Thank you, <%= full_name %>. Your subscription is active until <%= subscription.end_date.toDateString() %>.</p>
            <% } else if (status === 'failure') { %>
                <h1>Subscription Failed</h1>
                <p><%= message %></p>
                <p>Please check your payment details and try again.</p>
            <% } %>

            <!-- Button to return to the subscription page or dashboard -->
            <a href="/user/subscribe" class="button">Go Back</a>
        </main>
    </div>

    <footer>
        <div class="footer-content">
            <p class="footer-number">07477045595</p>
            <div class="footer-links">
                <a href="https://instagram.com" target="_blank"><i class="fa-brands fa-instagram fa-xl"></i></a>
                <a href="https://facebook.com" target="_blank"><i class="fa-brands fa-facebook fa-xl"></i></a>
                <a href="https://x.com" target="_blank"><i class="fa-brands fa-x-twitter fa-xl"></i></a>
            </div>
        </div>
    </footer>
</body>
</html>
